// This file is used to create a context for the authentication state
// It is used to provide the authentication state to the components that need it

import { createContext } from 'react';

export const AuthContext = createContext({});